<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Wypożyczalnia aut{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    {# Bootstrap 5 #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: #f8fafc; }
        .navbar-brand { font-weight: 700; letter-spacing: .5px; }
        .card { border: none; border-radius: 12px; box-shadow: 0 6px 24px rgba(0,0,0,.06); }
        .btn-rounded { border-radius: 999px; }
        .badge-pill { border-radius: 999px; padding: .4rem .65rem; }
        .muted { color:#6c757d; }
    </style>

    {% block stylesheets %}{% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg bg-white shadow-sm sticky-top">
    <div class="container">
        {# LOGO → lista aut #}
        <a class="navbar-brand" href="{{ path('app_car_list') }}">🚗 Rental</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMain">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div id="navMain" class="collapse navbar-collapse">
            <ul class="navbar-nav me-auto">
                <li class="nav-item"><a class="nav-link" href="{{ path('app_car_list') }}">Auta</a></li>
                {% if app.user %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_reservations') }}">Rezerwacje</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_my_cars') }}">Moje auta</a></li>
                {% endif %}
            </ul>

            <ul class="navbar-nav ms-auto">
                {% if app.user %}
                    <li class="nav-item me-2">
                        <a class="btn btn-outline-secondary btn-sm btn-rounded" href="{{ path('app_notifications') }}">🔔 Powiadomienia</a>
                    </li>
                    {# E-mail → PROFIL (nie rejestracja) #}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_profile') }}">👤 {{ app.user.email }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-outline-dark btn-sm btn-rounded ms-2" href="{{ path('app_logout') }}">Wyloguj</a>
                    </li>
                {% else %}
                    <li class="nav-item"><a class="btn btn-primary btn-sm btn-rounded" href="{{ path('app_login') }}">Zaloguj</a></li>
                    <li class="nav-item ms-2"><a class="btn btn-outline-primary btn-sm btn-rounded" href="{{ path('app_register') }}">Rejestracja</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<main class="container py-4">
    {% include '_partials/_flash.html.twig' %}
    {% block body %}{% endblock %}
</main>

<footer class="py-4 border-top bg-white">
    <div class="container small text-center text-muted">
        © {{ "now"|date("Y") }} Rental — prosta wypożyczalnia aut
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
