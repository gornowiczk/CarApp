{% extends 'base.html.twig' %}

{% block title %}Edytuj profil{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h2>✏️ Edytuj profil</h2>

        {{ form_start(form) }}
        <div class="row g-3">
            <div class="col-md-6">{{ form_row(form.firstName) }}</div>
            <div class="col-md-6">{{ form_row(form.lastName) }}</div>
            <div class="col-md-6">{{ form_row(form.phone) }}</div>
            <div class="col-md-6">{{ form_row(form.address) }}</div>
            <div class="col-md-12">{{ form_row(form.email) }}</div>
            <div class="col-md-12">{{ form_row(form.peselOrNip) }}</div>
            <div class="col-md-12">{{ form_row(form.plainPassword) }}</div>
        </div>

        <div class="mt-3">
            <button class="btn btn-success">💾 Zapisz zmiany</button>
            <a href="{{ path('app_profile') }}" class="btn btn-secondary">↩️ Powrót</a>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
